//calculo de  diametro
let c = 0, r = 4, ao = 0;

c = 2 * Math.PI * r;

console.log(`O comprimento do Circulo é: ${c.toFixed(2)}`);

//Calculo de área de circulo

ao = Math.PI * Math.pow(r, 2);

console.log(`A área do Circulo é: ${ao.toFixed(2)}`);